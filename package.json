{
  "name": "jest-test-toggler",
  "main": "./lib/jest-test-toggler",
  "version": "0.0.0",
  "description": "Toggle between jest tests and test subject files.",
  "keywords": [
    "jest", "test", "toggle"
  ],
  "activationCommands": {
    "atom-workspace": "jest-test-toggler:toggle"
  },
  "repository": "https://github.com/Fito/jest-test-toggler",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
